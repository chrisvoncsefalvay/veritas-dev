# VERITAS Reference Implementation - Dependencies

# Core cryptographic primitives
cryptography>=41.0.0

# Numerical computing
numpy>=1.24.0

# Optional: For real semantic embeddings (recommended for production)
# Uncomment to use real sentence transformers instead of mock embeddings
# sentence-transformers>=2.2.0
# torch>=2.0.0

# Optional: For compression (PCA, clustering)
# Uncomment for full compression functionality
# scikit-learn>=1.3.0

# Optional: For BLAKE3 hashing (faster alternative to SHA-256)
# Uncomment to use BLAKE3 instead of SHA-256
# blake3>=0.3.0

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
